/** @jsx React.DOM */

var React = require('react-atom-fork');

var Component = React.createClass({
  render: function () {
    return (
      <div>
      asdf
      </div>
    );
  }
});

module.exports = Component;